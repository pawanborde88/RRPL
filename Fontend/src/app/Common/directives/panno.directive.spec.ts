import { PANNoDirective } from './panno.directive';

describe('PANNoDirective', () => {
  it('should create an instance', () => {
    const directive = new PANNoDirective();
    expect(directive).toBeTruthy();
  });
});
